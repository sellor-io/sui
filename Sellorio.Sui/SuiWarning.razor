@using Sellorio.Results.Messages

<SuiResultMessages Result="result" />

@code {
	Result? result;

	[Parameter, EditorRequired]
	public string Message { get; set; }

	protected override void OnInitialized()
	{
		result = ResultMessage.Warning(Message);
	}
}
